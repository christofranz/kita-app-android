<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/eventDetailText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp" />

    <Button
        android:id="@+id/stayHomeButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="visible"
        android:text="Stay Home" />
    <TextView
        android:id="@+id/textViewFeedbackStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Feedback Status"
        android:textSize="18sp"
        android:layout_marginTop="16dp" />

    <!-- Withdraw Feedback Button -->
    <Button
        android:id="@+id/buttonWithdrawFeedback"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Withdraw Feedback"
        android:visibility="gone"
        android:layout_marginTop="8dp" />
</LinearLayout>
